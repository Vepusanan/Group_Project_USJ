{
  "name": "group-project-usj",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "VITE_API_URL=/api npm run build --workspace=client",
    "vercel-build": "VITE_API_URL=/api npm run build --workspace=client && node scripts/verify-vercel-build.mjs",
    "build:production": "npm ci && VITE_API_URL=/api npm run build --workspace=client",
    "start": "node server/server.js",
    "test": "npm test --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "ci": "npm ci && VITE_API_URL=/api npm run build --workspace=client && npm test --workspace=server"
  },
  "engines": {
    "node": "20.x"
  },
  "type": "module",
  "overrides": {
    "esbuild": "^0.25.0",
    "nodemailer": "^9.0.1"
  }
}
